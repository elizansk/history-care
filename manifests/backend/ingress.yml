apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: history-care-backend-ingress
spec:
  rules:
    - host: api.history-care.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: history-care-backend
                port:
                  number: 8080
