apiVersion: v1
kind: ConfigMap
metadata:
  name: history-care-grafana-datasources
data:
  datasources.yml: |
    apiVersion: 1

    datasources:
      - name: Prometheus
        type: prometheus
        access: proxy
        url: http://history-care-prometheus:9090
        isDefault: true
        editable: true
