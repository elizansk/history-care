apiVersion: v1
kind: ConfigMap
metadata:
  name: history-care-prometheus-conf
data:
  prometheus.yml: |
    global:
      scrape_interval: 5s

    scrape_configs:
      - job_name: "backend"
        metrics_path: /metrics
        static_configs:
          - targets:
              - "history-care-backend:8080"
